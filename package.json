{
  "name": "t3-wecom-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone webhook relay for WeCom application messages (Node)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:local": "node --env-file=.env server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@wecom/crypto": "^1.0.1",
    "fast-xml-parser": "^5.7.0"
  }
}
